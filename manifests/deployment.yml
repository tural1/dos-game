apiVersion : apps/v1
kind: Deployment
metadata:
  name: "testdos-311a"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: "testdos-311a"
  template:
    metadata:
      labels:
        app: "testdos-311a"
    spec:
      containers:
        - name: "testdos-311a"
          image: "tural.azurecr.io/testdos"
          ports:
          - containerPort: 80